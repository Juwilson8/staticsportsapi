// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id               String    @id @default(cuid())
  name             String    @default("")
  email            String    @unique @default("")
  password         String
  createdAt        DateTime? @default(now())
  from_Game_author Game[]    @relation("Game_author")
}

model Game {
  id               String   @id @default(cuid())
  title            String   @default("")
  content          String   @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  author           User?    @relation("Game_author", fields: [authorId], references: [id])
  authorId         String?  @map("author")
  from_Metric_game Metric[] @relation("Metric_game")

  @@index([authorId])
}

model MetricType {
  id                      String   @id @default(cuid())
  name                    String   @default("")
  from_Metric_metric_type Metric[] @relation("Metric_metric_type")
}

model Metric {
  id            String      @id @default(cuid())
  metric_value  String      @default("")
  metric_type   MetricType? @relation("Metric_metric_type", fields: [metric_typeId], references: [id])
  metric_typeId String?     @map("metric_type")
  game          Game?       @relation("Metric_game", fields: [gameId], references: [id])
  gameId        String?     @map("game")
  drive         Int
  play          Int
  second_mark   Int?

  @@index([metric_typeId])
  @@index([gameId])
}
